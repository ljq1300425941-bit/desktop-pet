# Desktop Pet

Electron + Vite desktop pet prototype for Windows.

## Scripts

```powershell
npm install
npm run dev
```

The first version opens a transparent, always-on-top pet window with click, drag, idle, and sleep states.
